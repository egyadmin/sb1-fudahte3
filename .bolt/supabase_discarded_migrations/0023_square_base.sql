/*
  # Empty Migration
  
  This migration is intentionally empty since we need to avoid version conflicts.
  Using version 0040 to ensure it's higher than any existing migration.
*/

-- Intentionally empty migration
SELECT 1;